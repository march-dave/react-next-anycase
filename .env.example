# Rename to .env.local and add your OpenAI key
OPENAI_API_KEY=
# Optional: override default model
# OPENAI_MODEL=gpt-4
# Optional: add a custom system message sent with every request
# OPENAI_SYSTEM_MESSAGE="You are a helpful assistant."
